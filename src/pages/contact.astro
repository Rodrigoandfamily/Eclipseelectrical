---
import BaseLayout from "../layouts/BaseLayout.astro";

const PHONE     = "+1 (416) 838-7519";
const PHONE_RAW = "+14168387519";
const EMAIL     = "info@eclipseelectricalservices.ca";

// Detect success redirect from Netlify
const success = Astro.url.searchParams.get("success") === "true";
---

<BaseLayout
  title="Contact Eclipse Electrical Services | Free Estimate Toronto GTA"
  description="Contact Eclipse Electrical Services for a free estimate in Toronto and GTA. Call +1 (416) 838-7519 or fill out our online form. 24-hour emergency available."
  currentPath="/contact"
>

<!-- ── PAGE BANNER ── -->
<section class="page-banner">
  <h1>Contact Us</h1>
  <p>Get your free, no-obligation estimate. Licensed Master Electricians serving Toronto &amp; GTA.</p>
</section>

<!-- ── CONTACT LAYOUT ── -->
<section class="contact-wrap" aria-label="Contact form and details">
  <div class="contact-inner">

    <!-- ── CONTACT FORM ── -->
    <div class="contact-form-col">
      <div class="section-label">Send a Message</div>
      <h2 class="section-h2">Request a Free Estimate</h2>
      <p class="contact-lead">Fill out the form below and we'll get back to you as soon as possible — usually within a few hours during business hours.</p>

      <!-- ── SUCCESS STATE ── -->
      {success && (
        <div class="form-success" role="alert" aria-live="polite">
          <svg viewBox="0 0 24 24" aria-hidden="true"><circle cx="12" cy="12" r="10"/><polyline points="16 8 10 14 8 12"/></svg>
          <div>
            <strong>Message sent!</strong>
            <p>Thank you for contacting Eclipse Electrical Services. We'll be in touch shortly.</p>
          </div>
        </div>
      )}

      <!-- ── CONTACT FORM ── -->
      {!success && (
        <form
          id="contact-form"
          name="contact"
          method="POST"
          action="/contact?success=true"
          data-netlify="true"
          netlify-honeypot="bot-field"
          novalidate
          aria-label="Contact form"
        >
          <input type="hidden" name="form-name" value="contact" />

          <!-- Honeypot field - hidden from real users -->
          <div style="display:none;visibility:hidden;position:absolute;" aria-hidden="true">
            <label>
              Don't fill this out if you're human:
              <input type="text" name="bot-field" tabindex="-1" autocomplete="off" />
            </label>
          </div>

          <div class="form-row">
            <div class="field-group">
              <label for="full-name">Full Name <span aria-hidden="true">*</span></label>
              <input
                type="text"
                id="full-name"
                name="name"
                required
                placeholder="Jane Smith"
                autocomplete="name"
              />
            </div>
            <div class="field-group">
              <label for="phone">Phone Number <span aria-hidden="true">*</span></label>
              <input
                type="tel"
                id="phone"
                name="phone"
                required
                placeholder="+1 (416) 555-0100"
                autocomplete="tel"
              />
            </div>
          </div>

          <div class="field-group">
            <label for="email">Email Address <span aria-hidden="true">*</span></label>
            <input
              type="email"
              id="email"
              name="email"
              required
              placeholder="jane@example.com"
              autocomplete="email"
            />
          </div>

          <div class="field-group">
            <label for="service-type">Type of Service <span aria-hidden="true">*</span></label>
            <select id="service-type" name="service_type" required>
              <option value="" disabled selected>— Select a service —</option>
              <option value="Residential Electrical">Residential Electrical</option>
              <option value="Commercial / Industrial">Commercial / Industrial</option>
              <option value="Emergency Service">Emergency Service</option>
              <option value="EV Charger Installation">EV Charger Installation</option>
              <option value="Switchboard Upgrade">Switchboard Upgrade</option>
              <option value="Safety Inspection">Safety Inspection</option>
              <option value="Lighting / Fans">Lighting / Fans</option>
              <option value="Other">Other</option>
            </select>
          </div>

          <div class="field-group">
            <label for="message">Message / Project Details <span aria-hidden="true">*</span></label>
            <textarea
              id="message"
              name="message"
              required
              rows="6"
              placeholder="Please describe your electrical needs — location, scope, any photos or measurements you have…"
            ></textarea>
          </div>

          <!-- Privacy Consent -->
          <div class="consent-row">
            <input type="checkbox" id="privacy-consent" name="consent" value="true" required />
            <label for="privacy-consent">
              I have read and agree to the <a href="/privacy">Privacy Policy</a>. I consent to Eclipse Electrical Services contacting me regarding my enquiry. <span aria-hidden="true">*</span>
            </label>
          </div>

          <button type="submit" class="btn-submit-full">
            <svg viewBox="0 0 24 24" aria-hidden="true"><line x1="22" y1="2" x2="11" y2="13"/><polygon points="22 2 15 22 11 13 2 9 22 2"/></svg>
            Send My Request
          </button>

          <p class="form-note">* Required fields. We typically respond within 2–4 business hours.</p>
        </form>
      )}
    </div><!-- /contact-form-col -->

    <!-- ── CONTACT INFO SIDEBAR ── -->
    <aside class="contact-info-col" aria-label="Contact information">
      <div class="info-card">
        <div class="info-card-header">
          <h3>Get In Touch</h3>
          <p>Prefer to talk? Our team is ready.</p>
        </div>

        <div class="info-items">
          <a href={`tel:${PHONE_RAW}`} class="info-item" aria-label="Phone number">
            <div class="info-icon">
              <svg viewBox="0 0 24 24" aria-hidden="true"><path d="M22 16.92v3a2 2 0 0 1-2.18 2 19.79 19.79 0 0 1-8.63-3.07A19.5 19.5 0 0 1 4.14 13.5a19.79 19.79 0 0 1-3.07-8.67A2 2 0 0 1 3.05 2h3a2 2 0 0 1 2 1.72c.127.96.361 1.903.7 2.81a2 2 0 0 1-.45 2.11L7.09 9.91a16 16 0 0 0 6 6l1.27-1.27a2 2 0 0 1 2.11-.45c.907.339 1.85.573 2.81.7A2 2 0 0 1 22 18z"/></svg>
            </div>
            <div>
              <strong>Phone</strong>
              <span>{PHONE}</span>
            </div>
          </a>

          <a href={`mailto:${EMAIL}`} class="info-item" aria-label="Email address">
            <div class="info-icon">
              <svg viewBox="0 0 24 24" aria-hidden="true"><path d="M4 4h16c1.1 0 2 .9 2 2v12c0 1.1-.9 2-2 2H4c-1.1 0-2-.9-2-2V6c0-1.1.9-2 2-2z"/><polyline points="22,6 12,13 2,6"/></svg>
            </div>
            <div>
              <strong>Email</strong>
              <span>{EMAIL}</span>
            </div>
          </a>

          <div class="info-item">
            <div class="info-icon">
              <svg viewBox="0 0 24 24" aria-hidden="true"><path d="M21 10c0 7-9 13-9 13S3 17 3 10a9 9 0 0 1 18 0z"/><circle cx="12" cy="10" r="3"/></svg>
            </div>
            <div>
              <strong>Service Area</strong>
              <span>Toronto &amp; GTA</span>
            </div>
          </div>
        </div>

        <!-- Hours -->
        <div class="hours-block">
          <h4>Business Hours</h4>
          <div class="hours-row"><span>Monday – Friday</span><span>8am – 8pm</span></div>
          <div class="hours-row"><span>Saturday</span><span>8am – 8pm</span></div>
          <div class="hours-row"><span>Sunday</span><span>Closed</span></div>
          <div class="emerg-badge">
            <span class="emerg-dot" aria-hidden="true"></span>
            24-Hour Emergency Service Available
          </div>
        </div>

        <!-- Trust badges -->
        <div class="trust-badges">
          <div class="trust-badge">
            <svg viewBox="0 0 24 24" aria-hidden="true"><path d="M12 22s8-4 8-10V5l-8-3-8 3v7c0 6 8 10 8 10z"/></svg>
            <span>Licensed &amp; Insured</span>
          </div>
          <div class="trust-badge">
            <svg viewBox="0 0 24 24" aria-hidden="true"><polyline points="20 6 9 17 4 12"/></svg>
            <span>Done Right the First Time</span>
          </div>
          <div class="trust-badge">
            <svg viewBox="0 0 24 24" aria-hidden="true"><circle cx="12" cy="12" r="10"/><polyline points="12 6 12 12 16 14"/></svg>
            <span>Fast Response</span>
          </div>
          <div class="trust-badge">
            <svg viewBox="0 0 24 24" aria-hidden="true"><line x1="12" y1="1" x2="12" y2="23"/><path d="M17 5H9.5a3.5 3.5 0 0 0 0 7h5a3.5 3.5 0 0 1 0 7H6"/></svg>
            <span>Competitive Pricing</span>
          </div>
        </div>
      </div>
    </aside>

  </div><!-- /contact-inner -->
</section>

</BaseLayout>

<style>
  /* ── CONTACT WRAP ── */
  .contact-wrap { padding: 5rem 2rem; background: var(--off-white); }
  .contact-inner {
    max-width: 1100px; margin: 0 auto;
    display: grid; grid-template-columns: 1fr 380px; gap: 4rem;
    align-items: start;
  }

  /* ── FORM COLUMN ── */
  .contact-lead { font-size: .97rem; line-height: 1.75; color: var(--mid); margin-bottom: 2rem; }

  form { display: flex; flex-direction: column; gap: 1.25rem; }
  .form-row { display: grid; grid-template-columns: 1fr 1fr; gap: 1.25rem; }

  .field-group { display: flex; flex-direction: column; gap: .35rem; }
  .field-group label {
    font-size: .8rem; font-weight: 600; color: var(--ink);
    letter-spacing: .04em;
  }
  .field-group label span { color: var(--red); }
  .field-group input,
  .field-group select,
  .field-group textarea {
    background: #fff; border: 1.5px solid rgba(0,0,0,.12); border-radius: 4px;
    padding: .75rem 1rem; font-family: "Open Sans", sans-serif;
    font-size: .9rem; color: var(--ink); outline: none;
    transition: border-color .18s, box-shadow .18s;
  }
  .field-group input::placeholder,
  .field-group textarea::placeholder { color: #bbb; }
  .field-group input:focus,
  .field-group select:focus,
  .field-group textarea:focus {
    border-color: var(--blue); box-shadow: 0 0 0 3px rgba(118,173,213,.2);
  }
  .field-group select { cursor: pointer; }
  .field-group textarea { resize: vertical; min-height: 150px; }

  .consent-row { display: flex; gap: .75rem; align-items: flex-start; }
  .consent-row input[type="checkbox"] {
    width: 18px; height: 18px; flex-shrink: 0;
    margin-top: 2px; cursor: pointer; accent-color: var(--blue-dark);
  }
  .consent-row label {
    font-size: .82rem; color: var(--mid); line-height: 1.6; cursor: pointer;
  }
  .consent-row label a { color: var(--blue-dark); text-decoration: underline; }
  .consent-row label span { color: var(--red); }

  .btn-submit-full {
    display: flex; align-items: center; justify-content: center; gap: .65rem;
    padding: 1rem 2rem; background: var(--red); color: #fff;
    font-family: "Montserrat", sans-serif; font-weight: 700;
    font-size: .9rem; letter-spacing: .1em; text-transform: uppercase;
    border: none; border-radius: 4px; cursor: pointer;
    transition: background .18s, transform .12s;
  }
  .btn-submit-full svg { width: 16px; height: 16px; stroke: #fff; fill: none; stroke-width: 2; stroke-linecap: round; }
  .btn-submit-full:hover { background: #a93226; transform: translateY(-1px); }

  .form-note { font-size: .75rem; color: var(--mid); }

  /* ── SUCCESS STATE ── */
  .form-success {
    display: flex; align-items: flex-start; gap: 1rem;
    background: #f0faf4; border: 2px solid #4ade80;
    border-radius: 6px; padding: 1.5rem; margin-bottom: 1.5rem;
  }
  .form-success svg { width: 28px; height: 28px; stroke: #16a34a; fill: none; stroke-width: 2.5; flex-shrink: 0; }
  .form-success strong { display: block; font-family: "Montserrat", sans-serif; font-weight: 800; font-size: 1rem; color: var(--ink); margin-bottom: .25rem; }
  .form-success p { font-size: .9rem; color: var(--mid); line-height: 1.6; margin: 0; }

  /* ── INFO SIDEBAR ── */
  .info-card {
    background: var(--ink); border-radius: 8px; overflow: hidden;
    position: sticky; top: 120px;
  }
  .info-card-header {
    background: var(--blue-dark); padding: 1.75rem 1.75rem 1.5rem;
  }
  .info-card-header h3 {
    font-family: "Montserrat", sans-serif; font-weight: 900;
    font-size: 1.1rem; color: #fff; margin-bottom: .3rem;
  }
  .info-card-header p { font-size: .85rem; color: rgba(255,255,255,.7); }

  .info-items { padding: 1.5rem 1.75rem; display: flex; flex-direction: column; gap: 1rem; }
  .info-item {
    display: flex; align-items: center; gap: .85rem;
    text-decoration: none; transition: opacity .15s;
  }
  a.info-item:hover { opacity: .8; }
  .info-icon {
    width: 40px; height: 40px; border-radius: 50%;
    background: rgba(255,255,255,.1); display: flex;
    align-items: center; justify-content: center; flex-shrink: 0;
  }
  .info-icon svg { width: 18px; height: 18px; stroke: var(--blue); fill: none; stroke-width: 2; stroke-linecap: round; }
  .info-item strong { display: block; font-size: .72rem; font-weight: 600; letter-spacing: .12em; text-transform: uppercase; color: rgba(255,255,255,.5); margin-bottom: .1rem; }
  .info-item span { font-size: .88rem; color: #fff; font-weight: 500; }

  .hours-block {
    padding: 1.25rem 1.75rem 1.5rem;
    border-top: 1px solid rgba(255,255,255,.07);
  }
  .hours-block h4 {
    font-family: "Montserrat", sans-serif; font-weight: 800; font-size: .72rem;
    letter-spacing: .18em; text-transform: uppercase; color: rgba(255,255,255,.4);
    margin-bottom: .85rem;
  }
  .hours-row {
    display: flex; justify-content: space-between;
    font-size: .83rem; color: rgba(255,255,255,.5);
    border-bottom: 1px solid rgba(255,255,255,.05); padding-bottom: .45rem;
    margin-bottom: .45rem;
  }
  .hours-row span:last-child { color: rgba(255,255,255,.85); font-weight: 500; }
  .emerg-badge {
    display: flex; align-items: center; gap: .5rem;
    font-size: .75rem; color: var(--blue); font-weight: 600;
    letter-spacing: .04em; margin-top: .75rem;
  }
  .emerg-dot {
    width: 7px; height: 7px; border-radius: 50%; background: #4ade80;
    animation: blink 2s ease-in-out infinite; flex-shrink: 0;
  }
  @keyframes blink { 0%,100%{ opacity:1 } 50%{ opacity:.3 } }

  .trust-badges {
    padding: 1.25rem 1.75rem 1.75rem;
    border-top: 1px solid rgba(255,255,255,.07);
    display: grid; grid-template-columns: 1fr 1fr; gap: .85rem;
  }
  .trust-badge {
    display: flex; align-items: center; gap: .5rem;
    font-size: .75rem; color: rgba(255,255,255,.6);
  }
  .trust-badge svg {
    width: 14px; height: 14px; stroke: var(--blue);
    fill: none; stroke-width: 2; stroke-linecap: round; flex-shrink: 0;
  }

  /* ── RESPONSIVE ── */
  @media (max-width: 900px) {
    .contact-inner { grid-template-columns: 1fr; gap: 3rem; }
    .info-card { position: static; }
  }
  @media (max-width: 600px) {
    .contact-wrap { padding: 3rem 1.25rem; }
    .form-row { grid-template-columns: 1fr; }
    .trust-badges { grid-template-columns: 1fr; }
  }
</style>
